# TEST API URL (deployed `server-test` app on Prisma Compute)
# Copy this file to `.env.development` (gitignored) and fill in real values.
# Loaded automatically by `npx expo start` (NODE_ENV=development), takes
# priority over `.env`.
EXPO_PUBLIC_API_URL=https://<test-app-id>.sin.prisma.build
